#!/usr/bin/env node

const { ktlint } = require('./executor');
const file = process.argv.length >= 3 ? process.argv[2] : '';

if (!file) {
  console.log('File is empty!');
  return;
}

ktlint(`-F ${file}`);

// For library user
exports.ktlint = ktlint;
