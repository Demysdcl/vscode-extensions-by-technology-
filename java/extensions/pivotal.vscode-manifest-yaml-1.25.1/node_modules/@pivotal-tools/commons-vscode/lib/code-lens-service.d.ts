import { CancellationToken, CodeLens, CodeLensProvider, Event, ProviderResult, TextDocument } from "vscode";
import { HighlightParams } from './highlight-service';
import * as Lsp from 'vscode-languageclient';
export declare class HighlightCodeLensProvider implements CodeLensProvider {
    private highlights;
    private _onDidChangeCodeLenses;
    get onDidChangeCodeLenses(): Event<void>;
    handle(highlghtParams: HighlightParams): void;
    static toVSCodeLens(cl: Lsp.CodeLens): CodeLens;
    provideCodeLenses(document: TextDocument, token: CancellationToken): ProviderResult<CodeLens[]>;
}
