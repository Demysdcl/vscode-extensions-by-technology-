import { Protocol } from '../protocol/generated/protocol';
export declare class StatusSeverity {
    static isOk(status: Protocol.Status): boolean;
    static isInfo(status: Protocol.Status): boolean;
    static isWarning(status: Protocol.Status): boolean;
    static isError(status: Protocol.Status): boolean;
    static isCancel(status: Protocol.Status): boolean;
    private static testSeverity;
}
